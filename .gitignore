node_modules/
.DS_Store
*.log
# Secrets — NEVER commit (this repo is public). Set via env / deploy.
.env
.env.local
data/
# imported galleries (server-side, from the NAS/poker vault — not committed)
public/tokens/*.webp
public/tokens/*.png
public/assets/characters/*.webp
public/assets/characters/*.png
