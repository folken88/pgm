node_modules/
.DS_Store
*.log
# Secrets — NEVER commit (this repo is public). Set via env / deploy.
.env
.env.local
data/
# public/tokens/ ART IS NOW COMMITTED (Tobias 2026-07-13: his own Midjourney art,
# so it's fine in this public repo — keeps rebuilds self-contained + backed up).
# Other imported galleries stay server-side (from the NAS/poker vault):
public/assets/characters/*.webp
public/assets/characters/*.png
.sync-backup/
.parity-backup/
